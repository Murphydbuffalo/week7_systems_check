<h3> Add A Manufacturer </h3>

<%= form_for @manufacturer do |f| %>
  <%= f.label :name %>:
  <%= f.text_field, :name %>

  <%= f.label :name %>:
  <%= f.text_field, :name %>

  <%= f.submit :'Add Manufacturer' %>
<% end %>
